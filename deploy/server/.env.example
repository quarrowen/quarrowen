# Copy to .env and edit. Names are the in-game names each person types in the menu ("Playing as").

# Shown in Multiplayer lists
SERVER_NAME=Family Server
SERVER_MOTD=Be kind, build big

# The game: vanilla (survival and creative building), or add-ons: vanilla,arcana,industry,guild
# Mods are loaded from MODS_DIR on this machine (created and filled on the first start). Drop a mod
# folder in there and restart to add it; SEED_MODS=missing keeps your own edits to the bundled ones.
GAME=vanilla
MODS_DIR=./mods
SEED_MODS=update
WORLD=family

# Admins (can use /gamemode, /tp, /allow, /ugc ...). Join once as this name before sharing the server:
# the first player to use a name keeps it.
ADMINS=Alex

# Only these players may join (admins always may). Add more later in game: /allow add Name
ALLOWLIST=Alex,Sam,Robin

# Player creations (painted skins, hats): approval = an admin approves them first (/ugc list, /ugc approve),
# auto = shown at once, off = none.
CREATIONS=approval

# Mask swear words in chat: on or off
CHAT_FILTER=on
